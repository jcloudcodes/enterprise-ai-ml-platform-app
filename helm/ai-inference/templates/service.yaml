apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-inference.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ai-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-inference.selectorLabels" . | nindent 4 }}
